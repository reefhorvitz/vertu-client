import React from 'react';

const ApartmentListTitle = ({title}) => {
    return (
        <div className="upcoming-tours container">
            <h1>{title}</h1>
        </div>
    );
}

export default ApartmentListTitle;